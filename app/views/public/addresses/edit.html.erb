<h2>配送先編集</h2>

<%= form_for(@address, url: address_path) do |f| %>

	<h4>郵便番号(ハイフンなし)<%= f.text_field :postcode %></h4>

	<h4>住所<%= f.text_field :address %></h4>

	<h4>宛名<%= f.text_field :name %></h4>

		<%= f.submit '変更を保存' %>
<% end %>