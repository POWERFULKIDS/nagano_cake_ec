<div class="customer-login col-md-6 col-md-offset-3">
<h4>会員の方はこちらからログイン</h4>

<ul>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <li class="email">
    <label><%= f.label :メールアドレス %></label>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </li>

  <li class="password">
    <label><%= f.label :パスワード %></label>
    <%= f.password_field :password, autocomplete: "current-password" %>
  </li>
  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <li class="pull-block"><%= link_to "=>パスワードを忘れた方はこちら", new_password_path(resource_name) %></li><br />
<% end %>
<li><%= f.submit "ログイン", class: "btn btn-secondary col-xs-7 login" %></li><br>
<% end %>
<li>登録がお済みでない方</li>
<li>
<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <p><%= link_to "こちら", new_registration_path(resource_name) %></p>
<% end %>
<p>から新規登録を行ってください。</p>
</li>
</ul>
</div>